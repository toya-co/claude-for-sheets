# Local TLS material for the loopback probe — regenerate, never commit
*.pem

# Apps Script local config (contains your own script ID)
addon/.clasp.json

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Internal design record and the Phase 0 probe harnesses. Kept locally, not
# published: the README carries everything a user or an installer needs, and
# these are the backend working notes behind it.
ARCHITECTURE.md
experiments/
