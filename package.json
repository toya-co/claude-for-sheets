{
  "name": "claude-sheets-sidebar",
  "version": "0.2.0",
  "description": "Claude chat sidebar for Google Sheets — Apps Script add-on plus a local companion app.",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "test": "node --test daemon/test/*.test.js addon/test/*.test.js scripts/test/*.test.js",
    "bundle": "node addon/bundle.js",
    "check": "node scripts/check.js"
  },
  "engines": {
    "node": ">=18"
  }
}
